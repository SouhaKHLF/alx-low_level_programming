#include "main.h"
/**
 * _strlen - returns the length of a string
 *
 * @s: string input parameter
 *
 * Return: length of a string
 */

void _strlen(char *s)
{
	int cnt;

	for (cnt = 0; *s != '\0'; s++)
		ctr++;
	return (ctr);
}
